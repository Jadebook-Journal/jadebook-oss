const config = {
  entries: [
    {
      filePath: "./src/index.ts", // Point to your primary source entry file
      outFile: "./dist/index.d.ts", // Output to your final declaration file
    },
  ],
};

export default config;
